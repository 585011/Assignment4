@model IEnumerable<BookRoom.Models.Rooms>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.beds)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.size)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.beds)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.size)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RoomsID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RoomsID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RoomsID })
        </td>
    </tr>
}

</table>
